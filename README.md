# 围棋对战教练

离线围棋对战 + 逐手讲解。内置蒙特卡洛树搜索（RAVE）引擎，不联网、不调用大模型。

- 网页版（iPad / 电脑）：`web/`，可"添加到主屏幕"后离线使用
- Android：`./build.sh` 用 Android SDK 命令行工具打包 WebView 外壳（无需 Gradle）

功能：9/13/19 路、5 档难度、AI 胜率控制（让棋模式）、让子与贴目、每手棋的好坏评价与原因、
更好的下法、提示、形势判断（地盘归属）、胜率曲线、点击点评回看局面、终局数子。
